"""
Pre-rewrite PyFastFlow codebase, retained as dead code for reference only.

None of this is on the supported path. It is the Taichi-only, single-backend
architecture the current `pyfastflow.core`-based stack replaces; `CONTEXT_legacy.md`
(repo root) documents its design. Kept in the tree so the port still has
something to read from - not imported by anything current, not maintained,
not guaranteed to run.

Author: B.G (08/2026)
"""
